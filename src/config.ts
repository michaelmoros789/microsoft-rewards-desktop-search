export const config = {
    DEFAULT_BOOKMARK_COUNT: 30,
    MAX_BOOKMARK_COUNT: 100,
    MIN_BOOKMARK_COUNT: 1,

    // Timing/delay settings
    DEFAULT_SKIP_DURATION: 10, // in seconds
    MIN_SKIP_DURATION: 1,
    MAX_SKIP_DURATION: 60,

    // Batch settings
    DEFAULT_BATCH_SIZE: 5,
    BATCH_PAUSE_SECONDS: 30,

    // Folder name (in case you want to make this dynamic)
    BOOKMARK_FOLDER_NAME: "Microsoft Rewards",
};