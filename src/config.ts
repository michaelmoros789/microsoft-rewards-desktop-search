export const config = {
    // Bookmark settings
    DEFAULT_BOOKMARK_COUNT: 2,
    MAX_BOOKMARK_COUNT: 100,
    MIN_BOOKMARK_COUNT: 1,

    // Timing/delay settings
    DEFAULT_SKIP_DURATION: 5, // in seconds
    MIN_SKIP_DURATION: 1,
    MAX_SKIP_DURATION: 60,

    // Batch settings
    DEFAULT_BATCH_SIZE: 1,
    MIN_BATCH_SIZE: 1,
    MAX_BATCH_SIZE: 50,
    DEFAULT_BATCH_INTERVAL: 10,
    MIN_BATCH_INTERVAL: 1,
    MAX_BATCH_INTERVAL: 7200,

    BOOKMARK_FOLDER_NAME: "Microsoft Rewards",
    DEFAULT_BASE_URL: "",
};